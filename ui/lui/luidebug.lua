


LUI.UIDebug = {}

LUI.UIDebug.NewDebugDrawTextItem = function(text, font, x, y, horzalign, vertalign, xScale, yScale, color, style)

end

LUI.UIDebug.NewDebugDrawImageItem = function(material, color, x, y, w, h, s0, t0, s1, t1, angle)

end

LUI.UIDebug.DrawElement = function(element)

end

LUI.UIDebug.DrawElementParent = function(element)

end

LUI.UIDebug.DrawElementChild = function(element)

end

LUI.UIDebug.DrawActiveElementCount = function(element)

end

LUI.UIDebug.DrawActiveElements = function(element)

end

LUI.UIDebug.DrawElementGlobalStatistics = function(element)

end

LUI.UIDebug.DrawElementStatistics = function(element)

end